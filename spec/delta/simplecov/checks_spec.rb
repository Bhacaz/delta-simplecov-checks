RSpec.describe Delta::Simplecov::Checks do
  it "has a version number" do
    expect(Delta::Simplecov::Checks::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
